记忆系统并非简单地将所有对话历史"存起来"，而是通过一套精心设计的判断规则来决定**什么值得记、记在哪里、记多久**，以及**用什么方式检索才能让最相关的记忆浮现**。这套规则分布在提示词工程层（agent_enhance.py 中的 120 行指令）、工具层（save_memory / search_memory 的参数校验与执行逻辑）、以及存储层（SQLite + FTS5 + 向量索引的三元结构）三个层面，共同构成了 Pocket-Agent 的记忆决策体系。

Sources: [agent/prompts/agent_enhance.py](agent/prompts/agent_enhance.py#L1-L122), [agent/tools/basic_tools.py](agent/tools/basic_tools.py#L753-L826)

---

## 记忆分层的三元判断

记忆系统的核心设计原则是**按使用场景分层**，不同层级的记忆有不同的存储目标、存储方式和生命周期。Agent 在接收到用户信息时，需要根据信息的性质判断应该存入哪一层。

### 三层记忆架构

| 层级 | 存储位置 | 典型内容 | 更新频率 | 生命周期 |
|------|---------|---------|---------|---------|
| **用户画像** | `user_profile.md` 文件 | 姓名、职业、长期偏好、行为约束 | 极少（只在用户明确纠正行为或提供永久信息时） | 永久保存，手动管理 |
| **事实记忆 (fact)** | messages 表 + FTS5 + 向量索引 | 技术选型、项目上下文、临时偏好 | 频繁（每次对话都可能产生） | 自动遗忘（30天 + 低重要性） |
| **事件记忆 (episodic)** | messages 表 + FTS5 + 向量索引 | 操作结果、问题解决方案、时间相关事件 | 中等 | 自动遗忘（30天 + 低重要性） |
| **舞蹈记忆 (dance)** | messages 表 + FTS5 + 向量索引 | 编舞数据、动作序列、姿态信息 | 低（专用于 3D 身体控制） | 自动遗忘，但自动检索时**被排除** |

这种分层的设计意图非常明确：用户画像是**永久信标**（极少写入，但每次对话都读入上下文），而消息表中的记忆是**动态知识库**（频繁写入，通过 FTS5 和向量索引实现精准检索）。舞蹈记忆被单独分出一类，是因为它的数据量通常很大（完整的动作序列 JSON），在自动注入上下文时默认排除，以避免浪费 token，但用户仍然可以通过 search_memory(memory_type="dance") 显式检索。

Sources: [agent/memory.py](agent/memory.py#L1-L64), [agent/prompts/agent_enhance.py](agent/prompts/agent_enhance.py#L85-L96), [agent/tools/basic_tools.py](agent/tools/basic_tools.py#L753-L826)

### 什么时候该存，什么时候不该存

提示词工程中的指令明确定义了记忆的触发条件——规则的核心思想是**宁滥勿缺，但有明确边界**。

**应当主动记忆的场景**（Agent 被要求**不要等待用户说"记住"**，而是自行判断）：
- 用户提到的个人信息：姓名、职业、习惯、偏好、目标、计划
- 用户的技术背景和能力水平
- 项目决定、技术选型、工作上下文
- 重要事件结果：部署成功/失败、问题解决方案
- 用户表达的不满、表扬、反馈、纠正行为（同时更新用户画像）
- 任何"下次对话时可能会有用"的信息

**禁止记忆的场景**：
- 普通闲聊、问候语
- 工具执行的技术细节（shell 输出、文件内容）
- 已经存储在用户画像中的信息（避免冗余）
- 一次性任务指令

判断标准被凝练为一句话：**这条信息下次对话时有没有用？有用就必须存。** 宁可多存一条无用的，也不要漏掉一条有用的。

Sources: [agent/prompts/agent_enhance.py](agent/prompts/agent_enhance.py#L111-L122), [agent/tools/basic_tools.py](agent/tools/basic_tools.py#L599-L614)

---

## 重要性评分与存储决策

每个记忆在保存时都会附带一个 `importance` 字段（1-10 的整数），这个分数不仅影响召回时的排序权重，还决定了记忆的生命周期长度。

### 重要性打分的参考标准

| 分值范围 | 含义 | 典型场景 | 使用频率 |
|---------|------|---------|---------|
| 1-2 | 临时性信息 | 一次性偏好、很快就会过时的上下文 | 极少使用高分 |
| 3-4 | 一般有用 | 用户的某个普通偏好、一次常规操作的结果 | **大多数记忆应落在此区间** |
| 5-6 | 比较重要 | 项目架构决定、关键技术选型 | 适当使用 |
| 7-8 | 很重要 | 影响后续多次决策的结论、重大问题的根因 | 谨慎使用 |
| 9-10 | 极其重要 | 用户明确强调的关键需求、不可逆的重大决定 | 极罕见使用 |

这里的核心规则是**不要每条都打高分**。Agent 提示词中明确要求自我约束：大多数记忆在 3-5 分即可，高分要留给真正值得反复回忆的内容。这个约束通过两个机制强制执行：
1. **RRF 融合排序中的重要性微调因子**：`importance_factor = 0.8 + importance * 0.04`，importance=5 时因子为 1.0，importance=3 时降为 0.92，importance=8 时也仅为 1.12——重要性对最终排序分的影响被限制在 ±20% 以内，防止低语义相关性但高重要性的记忆通过高分"爬"到前列。
2. **遗忘门限**：重要性 <= 2 且超过 30 天未访问的记忆，其向量索引会被清理，只有 FTS5 文本索引保留——这意味着这些记忆将无法通过语义搜索召回，仅能通过关键词匹配。

Sources: [agent/tools/basic_tools.py](agent/tools/basic_tools.py#L753-L820), [app.py](app.py#L149-L167), [agent/tools/basic_tools.py](agent/tools/basic_tools.py#L568-L574)

---

## 存储流程：从调用到落盘的三步写入

当 Agent 决定调用 `save_memory` 工具时，存储过程分为三个独立阶段，每个阶段涉及不同的存储引擎：

```mermaid
flowchart LR
    A[Agent 调用 save_memory] --> B{参数校验}
    B -->|type 必须是 fact/episodic/dance| C[写入 messages 表]
    B -->|importance 1-10| C
    
    C --> D[FTS5 自动同步]
    C --> E[手动触发向量索引]
    
    D --> F[messages_fts 虚拟表]
    D -->|INSERT 触发器| G[trigram 分词索引]
    
    E --> H[EmbeddingClient 生成向量]
    H --> I[embeddings 表存储 BLOB]
    
    C --> J[返回结果给 Agent]
    I --> J
```

### 第一阶段：messages 表写入

SQL 插入语句为：
```sql
INSERT INTO messages (conversation_id, role, content, timestamp, importance, memory_type) 
VALUES (?, ?, 'memory', ?, ?, ?)
```

所有类型的记忆都以 `role='memory'` 存入统一的 messages 表，与普通对话消息（`role='user'` 和 `role='assistant'`）共用同一张表。`memory_type` 字段（fact/episodic/dance）用于后续的类型过滤检索。

### 第二阶段：FTS5 全文索引自动同步

messages 表上注册了三个触发器，**自动**维护 `messages_fts` 虚拟表的同步：

- **INSERT 触发器**：新插入的 memory 行自动写入 FTS5 索引
- **DELETE 触发器**：删除时自动清理 FTS5 索引
- **UPDATE 触发器**：更新时先删后插，保持同步

FTS5 使用 `trigram` tokenizer，这是支持中文全文搜索的关键——它将每个连续的三个字符作为一个 token，使中文搜索无需预先分词即可工作：搜索"神经网络"时，FTS5 会自动拆解为"神经"、"经络"、"网络"等 trigram 匹配。

### 第三阶段：向量索引写入

向量索引**不是自动的**，由 `save_memory` 函数在写入 messages 表后**显式调用** `_vector_store.add()` 触发：
1. 通过 `_extract_embed_text()` 对记忆内容进行预处理——如果内容包含动作 JSON 数组（以 `[{"action":` 开头），只取 JSON 之前的文字描述部分，避免超长无语义内容膨胀向量空间。
2. 调用 EmbeddingClient（通过本地 llama-server 的 /embeddings 端点）生成向量。
3. 将 `(message_id, embedding_blob)` 存入独立的 `embeddings` 表——注意这里**只存 ID 和向量**，原始内容始终在 messages 表中，这是与 ChromaDB 等向量数据库的关键差异。

如果嵌入服务不可用，`_vector_store.add()` 会静默返回 False，记忆仍然成功存入 messages 表和 FTS5 索引——向量索引是可选的增强层，不是必需的。

Sources: [agent/tools/basic_tools.py](agent/tools/basic_tools.py#L790-L820), [app.py](app.py#L108-L144), [agent/embedding.py](agent/embedding.py#L1-L316)

---

## 检索时的判断逻辑：RRF 融合排序

当系统自动注入相关记忆（在 `_search_relevant_memories` 中）或用户主动调用 `search_memory` 时，检索过程采用**双通道 + 融合排序**的策略：

### 双通道检索

```mermaid
flowchart TD
    A[用户查询] --> B{选择检索通道}
    B -->|FTS5 通道| C[trigram 关键词搜索]
    B -->|向量通道| D[余弦相似度语义搜索]
    
    C --> E[结果集A：rank 排序]
    D --> F[结果集B：similarity 排序]
    
    E --> G[RRF 融合]
    F --> G
    
    G --> H[综合评分计算]
    H --> I[最终排序：取 top 3/5]
```

**FTS5 通道**（`_fts_search`）采用两级策略：
1. 优先使用 FTS5 trigram 搜索——只对长度 >= 3 的关键词有效，多个关键词用 OR 连接，取前 20 条按 rank 排序。
2. 短词（< 3 字符）或无 FTS5 结果时，回退到 LIKE 模糊搜索——对连续无空格的中文查询，按 2 字切分（如"动作舞蹈"→["动作", "舞蹈"]），使用 OR 连接的 LIKE 子句补充搜索。

**向量通道**（`_vector_search`）通过 EmbeddingClient 生成查询向量，然后在 `embeddings` 表中通过 numpy 批量计算余弦相似度。过滤条件（conversation_id、days、memory_type、msg_type）被下推到 SQL JOIN 层，避免对不相关数据计算相似度。相似度低于 0.35 的结果被丢弃。

### RRF 融合公式

```
score = 0.75 × (1/(k+rank+1)) × (0.5 + similarity) × importance_factor + 0.25 × recency_score
```

各分量的设计意图：
- **RRF 基础分** `1/(k+rank+1)` (k=60)：确保两个通道的结果都有公平的出场机会，排名靠前的结果获得更高基础分。
- **相似度加权** `(0.5 + similarity)`：向量结果的 similarity 在 0.35~1.0 之间，乘以基础分后语义相关性成为**主排序信号**。FTS5 结果没有 similarity 字段，用 0.5 兜底。
- **重要性微调** `importance_factor = 0.8 + importance × 0.04`：范围 0.84~1.2，仅作为辅助信号。
- **时间衰减** `recency_score = 0.995^(小时数)`：每小时衰减 0.5%，约 5.8 天后衰减到 50%，57 天后几乎归零。这确保**近期频繁访问**的记忆有天然优势。

最终，自动注入时取 top 3（过滤掉 dance 类型），主动搜索时取 top 5。

Sources: [agent/agent_langchain.py](agent/agent_langchain.py#L712-L734), [agent/tools/basic_tools.py](agent/tools/basic_tools.py#L386-L583), [agent/tools/basic_tools.py](agent/tools/basic_tools.py#L826-L900)

---

## 遗忘机制：自动清理策略

记忆系统不是无限增长的。`_cleanup_old_messages` 在应用启动时执行，清理规则很简单：

**保留策略**：`WHERE role = 'memory' AND importance <= 2 AND timestamp < 30天前` 的记忆，其**向量索引**被清理。

这里的关键细节是：**只清理 embeddings 表中的向量索引，不删除 messages 表中的原始记录，也不清理 FTS5 索引**。这意味着低重要性旧记忆依然可以通过关键词搜索召回，但失去了语义搜索能力。之所以保留 FTS5 索引，是因为：
1. FTS5 索引体积远小于向量索引（trigram token 是离散的，不需要存储浮点数向量）
2. 关键词搜索仍然可以作为"最后一线"的检索手段
3. 用户可能确实需要偶尔回溯旧对话

Sources: [app.py](app.py#L149-L167)

---

## 总结：记忆决策流程图

整个记忆系统的判断逻辑可以从 Agent 的视角归纳为以下流程：

```mermaid
flowchart TD
    A[用户发送消息] --> B{信息类型判断}
    B -->|永久个人信息| C[update_user_profile]
    B -->|项目/技术/偏好| D[save_memory type=fact]
    B -->|事件结果/方案| E[save_memory type=episodic]
    B -->|编舞/动作数据| F[save_memory type=dance]
    B -->|普通闲聊/工具输出| G[不记录]
    
    C --> H[写入 user_profile.md]
    D --> I[写入 messages + FTS5 + 向量]
    E --> I
    F --> I
    
    I --> J[自动注入 top 3 到上下文]
    I --> K[可被 search_memory 检索]
    I --> L[30天后低重要性→遗忘向量索引]
```

这个体系的设计哲学可以概括为：**存储时宽进严出（宁可多存），检索时语义优先（RRF 多信号融合），遗忘时渐进降级（先丢向量索引，保留文本索引）**。三层存储（原始消息 → FTS5 文本索引 → 向量语义索引）形成了从"低成本保底"到"高精度检索"的递进关系，而遗忘机制从反方向确保存储成本可控。

**继续阅读**：[记忆工具使用（save_memory / search_memory / update_user_profile）](19-ji-yi-gong-ju-shi-yong-save_memory-search_memory-update_user_profile) 了解这三个工具的具体参数与调用方式；[记忆清理与向量索引维护](21-ji-yi-qing-li-yu-xiang-liang-suo-yin-wei-hu) 深入了解遗忘机制的实现细节。