import requests
import time

OLLAMA_URL = "http://localhost:11434"

# 不同长度的测试文本
test_texts = {
    "短文本 (50字)": "你好，今天天气怎么样？",
    
    "中文本 (200字)": "请帮我记一下，明天下午三点要和客户开会，讨论产品上线方案。会上需要准备演示文稿，还要确认一下服务器部署的时间。另外记得提醒我买明天的机票，目的地是北京，最好是上午的航班。",
    
    "长文本 (500字)": """人工智能是计算机科学的一个分支，致力于创建能够执行通常需要人类智能的任务的系统。这些任务包括学习、推理、问题解决、知觉和语言理解等。
    
    机器学习是人工智能的一个子领域，它专注于开发能够从数据中学习的算法。深度学习则是机器学习的一个分支，它使用多层神经网络来模拟人类大脑的工作方式。
    
    近年来，大语言模型如GPT系列、Claude和文心一言等取得了巨大突破。这些模型通过在大量文本数据上训练，能够理解和生成自然语言，完成翻译、摘要、问答等多种任务。
    
    然而，大语言模型也存在局限性，如可能产生幻觉（即生成不符合事实的内容）、存在偏见、缺乏真正的理解能力等。研究者们正在努力解决这些问题，例如通过检索增强生成（RAG）技术，让模型能够查询外部知识库来减少幻觉。
    
    在你的手机Agent项目里，你正在使用Qwen3-4B作为对话模型，EmbeddingGemma作为向量化模型，准备实现长期记忆功能。这个架构属于RAG的一种实现方式：先将用户输入向量化，从向量数据库中检索相关记忆，再将这些记忆作为上下文提供给大模型，从而让模型具备持续学习和记忆的能力。""",
    
    "超长文本 (1000字)": """# 长期记忆系统的技术原理与实现方案

## 一、为什么需要长期记忆

传统的大语言模型在对话中只能记住当前会话窗口内的内容（通常几K到几十K tokens），一旦对话结束或超出窗口，之前的对话内容就会“遗忘”。这限制了模型作为Agent的能力，尤其是在需要持续学习和适应用户的场景。

长期记忆系统的目标是：让模型能够在多个会话之间保留关键信息，并在需要时自动检索和利用这些信息。

## 二、技术架构

现代Agent的长期记忆系统通常包含以下组件：

1. **Embedding模型**：将文本转换为高维向量，捕捉语义信息
2. **向量数据库**：存储和检索向量，支持相似性搜索
3. **记忆管理策略**：决定什么该记、什么该忘、什么时候检索
4. **上下文注入**：将检索到的记忆拼接到提示词中

## 三、Embedding模型选型

针对手机端部署，Embedding模型的参数量通常在300M-1B之间。你的选择EmbeddingGemma-300M是Google最新开源的端侧模型，具备以下特点：

- 308M参数，量化后仅200-300MB
- 支持768维输出，且可通过MRL技术降维
- MTEB评测在同规模模型中排名第一
- 支持100+语言，中文友好

## 四、向量数据库对比

### sqlite-vec
- 优势：零依赖、与SQLite无缝集成、内存占用极低
- 缺点：使用暴力搜索，数据量大时性能下降
- 适合：几千条记忆的轻量场景

### VexDB-Lite
- 优势：使用HNSW索引算法，大数据量下性能极佳
- 缺点：需要额外依赖、存储空间稍大
- 适合：追求极速检索的场景

### ObjectBox
- 优势：全能型数据库，同时支持普通数据和向量
- 缺点：API较重、学习成本高
- 适合：需要统一存储方案的场景

## 五、记忆检索流程

1. 用户输入 → Embedding模型 → 查询向量
2. 向量数据库 → top-k相似记忆（按距离排序）
3. 格式化记忆文本 → 拼接到系统提示词
4. 大模型 → 生成带记忆的回复
5. 异步处理：提取新事实 → 生成向量 → 存入数据库

## 六、你的当前配置

- 设备：Xiaomi 17 Pro，骁龙8至尊版，16GB内存
- 主模型：Qwen3-4B Q4_K_M (2.5GB)
- Embedding：EmbeddingGemma-300m BF16 (622MB)
- 向量数据库：待选择

## 七、性能测试结果

经过实测，你的手机上EmbeddingGemma-300m处理普通文本（几十字）约耗时230ms（CPU模式），由于Termux沙箱限制无法使用GPU加速。对于更长文本，耗时会随token数线性增加。

预计速度：
- 100字：~200-250ms
- 500字：~400-500ms
- 1000字：~700-1000ms

## 八、优化建议

1. 对于短文本（用户问题/简短记忆）：直接embedding
2. 对于长文本（需要存储的文档）：先做摘要，再embedding摘要
3. 考虑使用异步处理，不让embedding阻塞主对话
4. 批量处理：多条记忆可以一起向量化，提高吞吐量"""
}

print("=" * 60)
print("EmbeddingGemma-300m 长文本性能测试")
print("=" * 60)

for name, text in test_texts.items():
    # 计算字符数和估计 token 数
    char_count = len(text)
    est_tokens = char_count // 2  # 粗略估计
    
    print(f"\n📝 {name}")
    print(f"   字符数: {char_count}, 估计 tokens: ~{est_tokens}")
    
    # 预热
    requests.post(f"{OLLAMA_URL}/api/embeddings",
                  json={"model": "embeddinggemma:300m", "prompt": "预热"})
    time.sleep(0.5)
    
    # 测试 3 次取平均
    times = []
    for i in range(3):
        start = time.time()
        resp = requests.post(f"{OLLAMA_URL}/api/embeddings",
                             json={"model": "embeddinggemma:300m", "prompt": text})
        elapsed = (time.time() - start) * 1000
        times.append(elapsed)
    
    avg_time = sum(times) / len(times)
    print(f"   ⏱️  平均耗时: {avg_time:.0f} ms")
    print(f"   📊 耗时范围: {min(times):.0f} - {max(times):.0f} ms")

print("\n" + "=" * 60)
print("测试完成")