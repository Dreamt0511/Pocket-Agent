Agent 之所以"聪明"，不仅因为它能理解你说的话，更因为它能**记住**你说过的话，并在未来合适的时机回忆起来。Pocket-Agent 提供了三个核心记忆工具——`save_memory`、`search_memory`、`update_user_profile`——分别对应记忆的**写入**、**读取**和**用户画像维护**。这些工具由 Agent 的 LLM 在对话中自主判断、按需调用，构成了记忆系统的操作层。本文从初学者视角，逐一拆解每个工具的设计意图、参数含义和实际用法。

Sources: [basic_tools.py](agent/tools/basic_tools.py#L585-L930), [memory.py](agent/memory.py#L1-L64)

## save_memory — 记住值得记住的事

`save_memory` 是记忆系统的"写入"工具。每当对话中出现**值得以后回顾的信息**时，Agent 会自动调用它来保存。它的设计遵循一个关键原则：**不是每条对话都要存，只在真正有意义时才调用**。

### 参数说明

| 参数 | 类型 | 必填 | 默认值 | 含义 |
|------|------|------|--------|------|
| `content` | string | ✅ | — | 要记忆的内容，语言简洁明确 |
| `type` | string | ❌ | `"fact"` | 记忆类型：`"fact"` 事实 / `"episodic"` 事件 / `"dance"` 舞蹈动作 |
| `importance` | int | ❌ | `5` | 重要性 1~10，大部分记忆在 3~5 分即可 |

记忆分为三种类型，各自对应不同的用途：

- **`fact`（事实记忆）**：记录用户的长期偏好、技术选型、项目架构决定等**知识性**信息。例如"用户使用 Python 3.12 开发后端"、"用户偏好简洁的回答风格"。这类记忆的检索频率最高，价值最持久。
- **`episodic`（事件记忆）**：记录一次性但重要的**操作结果**，如"部署成功，服务运行在 8080 端口"、"数据库迁移报错，原因是磁盘空间不足"。之后遇到类似问题时可快速回溯。
- **`dance`（舞蹈动作记忆）**：专门记录编舞数据、动作序列、姿态信息。这类记忆在自动上下文注入时会被**跳过**（因其数据量大且特殊性高），需要 Agent 通过 `search_memory` 显式检索。

### 重要性打分参考

重要性评分是 Agent 自主判断的，不需要用户操心。初学者理解评分范围即可：

- **1~2 分**：临时性信息，很快会过时。如"用户今天要喝咖啡"。
- **3~4 分**：一般有用。如"用户喜欢用 VS Code 写代码"。
- **5~6 分**：比较重要。如"项目采用 FastAPI + SQLite 架构"。
- **7~8 分**：很重要，影响后续多次决策。如"用户要求所有操作必须手动确认才能执行"。
- **9~10 分**：极其重要，不可逆的重大决定。如"用户明确要求永远不要删除生产数据库"。

### 背后的存储流程

`save_memory` 被调用后，内部会执行两步操作（请见下图），保证记忆既能通过关键词搜索到（SQLite FTS5），也能通过语义搜索到（向量索引）：

```mermaid
flowchart LR
    A[Agent 调用 save_memory] --> B{验证参数}
    B -->|type 非法| C[返回错误提示]
    B -->|参数合法| D[写入 messages 表\n role='memory']
    D --> E[获取自增 msg_id]
    E --> F[调用 _extract_embed_text\n 提取嵌入文本]
    F --> G[生成向量 embedding\n 存入 embeddings 表]
    G --> H[返回成功消息\n 含耗时信息]
```

写入 `messages` 表时，记录的角色为 `"memory"`，并携带 `importance` 和 `memory_type` 字段。之后通过 `msg_id`（messages 表的自增 ID）作为关联键，将向量嵌入存入 `embeddings` 表。这里有一个细节值得注意：`_extract_embed_text` 函数会检测内容中是否包含动作 JSON 数据（如 `[{"action": ...}]`），如果检测到，它会只取 JSON 之前的文字描述部分用于嵌入，避免超长无效内容占用向量空间。

Sources: [basic_tools.py](agent/tools/basic_tools.py#L735-L825)

## search_memory — 找回被遗忘的细节

`search_memory` 是记忆系统的"读取"工具。它的特殊之处在于：**系统每次对话前会自动检索 top 3 相关记忆注入上下文**（标记为 `[相关记忆]`），但以下情况需要 Agent **主动调用**本工具进行深度搜索：

- 自动注入的 `[相关记忆]` 不够用，需要更多结果
- 用户明确提到"之前""上次""记得"等回溯关键词
- 需要按类型过滤（只搜事实 / 只搜事件 / 只搜舞蹈动作）
- 需要按时间范围过滤（如"最近 7 天"）
- 需要搜索**当前会话**的原始对话记录（`scope="session"`）

### 参数说明

| 参数 | 类型 | 必填 | 默认值 | 含义 |
|------|------|------|--------|------|
| `query` | string | ✅ | — | 搜索内容，**优先用完整句子**保留语义 |
| `scope` | string | ❌ | `"memory"` | `"memory"` 搜索跨会话记忆 / `"session"` 搜索当前会话 |
| `memory_type` | string | ❌ | None | 仅 `scope="memory"` 时生效，`"fact"` / `"episodic"` / `"dance"` |
| `days` | int | ❌ | None | 时间过滤，只返回过去 N 天内的结果 |

### scope 参数详解

**`scope="memory"`（默认）**：跨会话记忆搜索。这是最常用的模式，采用**双通道检索 + RRF 融合排序**策略：

1. **FTS5 关键词搜索**：先用 SQLite 的全文搜索引擎对 `messages` 表做 trigram 分词搜索。对长度 ≥ 3 字符的关键词直接匹配 FTS5 索引；对短词或 FTS5 无结果时，用 LIKE 模糊查询补充。中文无空格的长句会按 2 字一组自动切分（如"动作舞蹈"→["动作", "舞蹈"]）。
2. **向量语义搜索**：同时用嵌入服务将 `query` 转为向量，在 `embeddings` 表中通过 numpy 批量计算余弦相似度，找出语义相近的记忆（最低相似度阈值 0.35）。
3. **RRF 融合排序**：两种搜索结果按融合排序算法（Reciprocal Rank Fusion）合并，综合考虑语义相关性（权重 75%）、时间衰减（权重 25%）和重要性微调，最终返回 top 5 条最相关的记忆。

**`scope="session"`**：搜索当前会话的原始对话记录。不走向量搜索，只用关键词匹配当前会话的消息（排除 `role='memory'` 的记录），返回最多 5 条。这个模式主要用于 Agent 回溯"用户刚才说了什么"——当对话很长时，Agent 可能"忘记"几分钟前用户交代的细节，通过此模式可以精准定位。

```mermaid
flowchart TD
    S[search_memory 被调用] --> C{scope?}
    C -->|memory| D1[FTS5 关键词搜索]
    C -->|memory| D2[向量语义搜索\n余弦相似度]
    C -->|session| E[仅 FTS5 搜索\n当前会话]
    D1 --> F1[关键词结果集]
    D2 --> F2[向量结果集]
    F1 & F2 --> G[RRF 融合排序\n语义75% + 时间25%]
    G --> H[返回 top 5 记忆]
    E --> I[返回 top 5 当前会话消息]
```

Sources: [basic_tools.py](agent/tools/basic_tools.py#L826-L930)

## update_user_profile — 维护用户画像

与前两者不同，`update_user_profile` 不操作 `messages` 数据库，而是维护一个独立的**用户画像文件** `memory/user_profile.md`。这个文件以 Markdown 格式存储，用户可以在 Termux 中直接打开编辑，既保证可维护性，也方便用户理解 Agent 记住了自己的哪些信息。

### 参数说明

| 参数 | 类型 | 必填 | 含义 |
|------|------|------|------|
| `section` | string | ✅ | 画像章节名，可用预设或自定义名称 |
| `content` | string | ✅ | 该章节要记录的内容 |

### 预设章节

工具提供了五个预设章节，但也可以随意传入新的章节名来自定义：

| 章节名 | 用途 | 示例内容 |
|--------|------|----------|
| `基本信息` | 姓名、职业、位置、兴趣 | "姓名：张三，职业：后端开发工程师" |
| `沟通偏好` | 回答风格、语言习惯 | "偏好简洁回答，不要格式化的列表" |
| `行为要求` | 对 Agent 的约束和规则 | "执行任何操作前必须等我确认" |
| `反馈` | 用户对 Agent 表现的评价 | "上次总结太长，请缩短到 3 条以内" |
| `其他` | 不属于以上分类的信息 | "正在做毕业设计，主题是智能家居" |

### 工作流程

`update_user_profile` 被调用时，底层实际执行的是 `LongTermMemory.update_user_profile()` 方法。它先读取现有的 `user_profile.md` 文件全文，按 Markdown 的 `## 章节目录` 格式逐行解析：如果找到与 `section` 同名的标题，则替换其下方内容；如果没找到，则在文件末尾追加新章节。整个操作在异步线程中执行，不会阻塞对话流程。

```mermaid
flowchart LR
    A[Agent 调用 update_user_profile] --> B[读取 user_profile.md]
    B --> C{找到 ## 章节?}
    C -->|是| D[替换章节内容]
    C -->|否| E[在文件末尾追加新章节]
    D --> F[写回文件]
    E --> F
    F --> G[返回 ✅ 已更新用户画像]
```

Sources: [basic_tools.py](agent/tools/basic_tools.py#L585-L621), [memory.py](agent/memory.py#L1-L64)

## 三个工具的协作关系

这三个工具虽然各自独立，但在实际运行时形成了有机的配合：

**`update_user_profile` 负责"静态画像"**，记录用户的身份特征和行为约束——这些信息基本不变，每次对话都会以固定格式注入到系统提示词中，影响 Agent 的整体行为风格。

**`save_memory` 负责"动态记忆"**，记录具体的项目决定、事件结果和动作数据——这些信息会累积增长，通过自动检索按需注入。

**`search_memory` 负责"深度召回"**，当自动注入的记忆不够用时，进行更深入的搜索。

```
┌─────────────────────────────────────┐
│          系统初始化流程               │
│                                     │
│  app.py 启动 → 创建 VectorStore      │
│  → set_memory_refs(向量库, 数据库路径)│
│  → 创建 LongTermMemory              │
│  → set_memory_instance(记忆实例)     │
│  → 补录缺失的向量索引                │
└─────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────┐
│        每次对话前自动注入             │
│                                     │
│  _search_relevant_memories(query)   │
│  → FTS5 + 向量 → RRF 融合           │
│  → 取 top 3（跳过 dance 类型）       │
│  → 格式化为 [相关记忆] 注入上下文      │
└─────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────┐
│      对话中 Agent 自主调用工具         │
│                                     │
│  save_memory      → 写入新记忆       │
│  search_memory    → 深度搜索         │
│  update_user_profile → 更新画像      │
└─────────────────────────────────────┘
```

Sources: [agent_langchain.py](agent/agent_langchain.py#L710-L735), [app.py](app.py#L200-L225)

## 常见使用场景

### 场景一：用户表达长期偏好
> 用户："我平时用 Vim 写代码，不喜欢 IDE。"
> 
> Agent 的决策路径：
> 1. 先调用 `update_user_profile`，在"沟通偏好"章节记录"使用 Vim 编写代码，不喜欢 IDE"
> 2. 再调用 `save_memory(type="fact", importance=4)`，保存"用户偏好 Vim 编辑器"

### 场景二：回溯之前的决定
> 用户："我们上次数据库选型选了哪个？"
> 
> Agent 的决策路径：
> 1. 自动注入的 `[相关记忆]` 中可能已有答案
> 2. 如果没有，主动调用 `search_memory(query="数据库选型决定", scope="memory", memory_type="fact")`

### 场景三：行为约束
> 用户："以后任何删除操作都要先问我。"
> 
> Agent 的决策路径：
> 1. 调用 `update_user_profile(section="行为要求", content="所有删除类操作必须先获得用户明确确认才能执行")`
> 2. 这份约束在每次对话的系统提示词中都会出现，持续影响 Agent 的行为

Sources: [basic_tools.py](agent/tools/basic_tools.py#L753-L930)

## 深入阅读

理解这三个工具后，建议继续探索以下主题：

- **[记忆判断规则与存储策略](20-ji-yi-pan-duan-gui-ze-yu-cun-chu-ce-lue)**：深入学习 Agent 如何判断"什么值得记、什么不值得记"，以及 FTS5 和向量索引的底层存储细节
- **[分层记忆架构（用户画像 + 事实记忆 + 事件记忆）](15-fen-ceng-ji-yi-jia-gou-yong-hu-hua-xiang-shi-shi-ji-yi-shi-jian-ji-yi)**：从更高维度理解记忆系统的整体设计哲学
- **[RRF 融合排序策略](18-rrf-rong-he-pai-xu-ce-lue)**：深入 search_memory 背后使用的融合排序算法原理
- **[SQLite + FTS5 全文搜索](16-sqlite-fts5-quan-wen-sou-suo-trigram-zhong-wen-zhi-chi)**：了解 trigram 分词如何支持中文搜索