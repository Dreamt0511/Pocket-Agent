终端是 Pocket-Agent 与用户交互的核心战场。为了在狭小的终端空间中呈现清晰、美观且信息密度合理的界面，系统构建了一套基于 Rich 库的 UI 框架。这套框架涵盖从启动横幅、对话输入、进度反馈到结束统计的完整生命周期。本文深入剖析 `agent/ui.py` 中的 `PocketUI` 与 `ProgressDisplay` 两大核心类，并展示它们如何与 `agent_langchain.py` 中的执行流程编织成一个整体。

Sources: [agent/ui.py](agent/ui.py)

## 架构总览：三层的 UI 渲染管线

整个终端 UI 系统可以分为三个逻辑层，每层解决不同粒度的显示问题：

```mermaid
flowchart TD
    subgraph 应用层["🎮 应用层 (main.py / agent_langchain.py)"]
        A[run_conversation] --> B[创建 ProgressDisplay]
        A --> C[创建 Markdown Live]
        D[main 主循环] --> E[PocketUI 实例化]
        D --> F[prompt_toolkit 输入]
    end

    subgraph 组件层["🧩 组件层 (agent/ui.py)"]
        G[PocketUI] --> H[print_banner / print_welcome]
        G --> I[print_tool_call / print_tool_result]
        G --> J[print_agent_response / print_stream_chunk]
        G --> K[format_context_bar]
        G --> L[create_progress_display]
        M[ProgressDisplay] --> N[_TimerRenderable]
        M --> O[Live 容器]
        P[SlashCompleter] --> Q[/命令补全]
        P --> R[技能名补全]
    end

    subgraph 渲染层["🎨 渲染层 (Rich 库)"]
        S[Console] --> T[Panel / Rule]
        S --> U[Markdown 渲染]
        S --> V[Table]
        W[Live] --> X[transient 自动消失]
        W --> Y[refresh_per_second 控制]
    end

    应用层 --> 组件层
    组件层 --> 渲染层
```

这一分层的核心设计思想是：**应用层只关心何时显示什么信息**，组件层封装显示逻辑与样式，渲染层由 Rich 库提供高性能的终端输出能力。`PocketUI` 作为组件层的门面类，对外提供约 20 个命名清晰的打印方法，覆盖了项目所有需要终端输出的场景。

Sources: [agent/ui.py#L1-L99](agent/ui.py#L1-L99), [agent/agent_langchain.py#L958-L1242](agent/agent_langchain.py#L958-L1242)

## PocketUI：全场景的终端门户

`PocketUI` 类以 `Console` 为核心输出对象，围绕对话的生命周期提供了完整的方法集。按照功能领域，可以划分为以下模块：

| 功能域 | 核心方法 | 渲染方式 | 典型场景 |
|--------|----------|----------|----------|
| 启动引导 | `show_welcome_screen()` / `print_banner()` | `Panel` + 青色 Text | 程序启动时展示项目 Logo 与模型信息 |
| 状态指示 | `print_embed_status()` | 暗灰色文字 | 显示语义记忆是否可用 |
| 工具调用 | `print_tool_call()` / `print_tool_result()` | 绿色/青色 Panel | Agent 调用工具时展示名称与参数 |
| 思考过程 | `print_agent_thinking()` | 黄色 Panel | Agent 正在推理时给出等待提示 |
| 回复渲染 | `print_agent_response()` | `Markdown` 渲染 | AI 最终回复以 Markdown 格式显示 |
| 流式输出 | `print_stream_chunk()` | 青色逐字打印，`no_wrap=True` | 打字机效果的流式输出 |
| 记忆展示 | `print_memory_info()` | 品红色 Panel | 展示检索到的记忆内容 |
| 上下文条 | `format_context_bar()` | Unicode 进度条 + 颜色渐变 | 每次对话结束后展示 Token 用量 |
| 错误信息 | `print_error()` | 红色 Panel | 异常提示与错误报告 |
| 对话统计 | `print_conversation_stats()` | 暗青色 Panel | 展示工具调用次数与可用工具数 |

这些方法在设计上有两个共同点：**统一的 Panel 边框样式**让同类信息在视觉上形成组块，**Markdown 原生渲染**让 AI 回复中的标题、列表、代码块等结构化内容得以清晰呈现。`print_agent_response()` 直接调用 Rich 的 `Markdown` 渲染器，无需手动处理格式标记。

Sources: [agent/ui.py#L40-L260](agent/ui.py#L40-L260)

## ProgressDisplay：精密的时间感知进度引擎

进度显示系统是终端 UI 中最具技术含量的部分。`ProgressDisplay` 类没有采用简单的 `print()` 追加输出，而是构建了一个**自驱动的时间感知渲染器**，其核心机制如下：

```mermaid
sequenceDiagram
    participant App as run_conversation
    participant PD as ProgressDisplay
    participant Timer as _TimerRenderable
    participant Live as Rich Live

    App->>PD: __enter__()
    PD->>Live: Live(_TimerRenderable, refresh_per_second=4)
    Live->>Timer: 每 0.25 秒调用 __rich_console__
    Timer->>Timer: 计算 elapsed = now - step_start_time
    Timer-->>Live: "⏳ 思考中 [已耗时 3 秒]"
    
    App->>PD: update("调用工具: get_weather")
    PD->>Timer: 更新 current_step + 重置 step_start_time
    Timer-->>Live: "⏳ 调用工具: get_weather [已耗时 1 秒]"

    App->>PD: 收到首个回复 token
    PD->>Live: __exit__()
    Note over Live: transient=False 保留最后状态

    App->>Live: 创建新的 Live(Markdown)
    Note over Live: 开始流式渲染 AI 回复
```

`ProgressDisplay` 的巧妙之处在于 `_TimerRenderable` 这个辅助类。它实现了 Rich 的 `__rich_console__` 协议，使得 `Live` 容器在每次刷新时（默认 4 次/秒）自动重新计算当前步骤的已耗时，**无需外部线程或定时器驱动**。当步骤切换时（即调用 `update(new_step)`），`step_start_time` 被重置，时间显示自然从零开始计数。

关键设计决策包括：
- **不设 `transient=True`**：进度行在完成后保留在终端上，作为执行历史的可见痕迹。这与传统做法不同，因为用户在调试时需要看到完整的执行链路。
- **4Hz 刷新率**：在终端性能和流畅度之间取得平衡，既不会造成 CPU 浪费，又能让秒级时间更新看起来连续。
- **异常安全退出**：`__exit__` 被设计为可以安全地在 `except` 块中多次调用，确保任务取消、键盘中断或异常崩溃时都不会留下悬挂的 Live 实例。

Sources: [agent/ui.py#L344-L425](agent/ui.py#L344-L425)

## 双阶段输出切换逻辑：从进度条到 Markdown 的无缝过渡

在 `run_conversation()` 中，存在一个关键的**输出模式切换点**。执行过程分为两个阶段：

**阶段一（进度追踪）**：Agent 在思考或调用工具时，`ProgressDisplay` 占据终端底部的一行，实时反映当前步骤。此时所有输出都浓缩在这一行中，不干扰终端的历史信息。

**阶段二（流式回复）**：一旦 `astream` 的 `messages` 通道返回了第一个非空的 AI 增量内容，系统立即执行切换：

```python
# 关闭进度显示
if progress_display:
    progress_display.__exit__(None, None, None)
    progress_display = None

# 启动 Markdown Live 渲染
stream_live = Live(Markdown(""), console=self.ui.console, refresh_per_second=10)
stream_live.__enter__()
```

切换时，旧的 `ProgressDisplay` 被关闭（其最后一行保留在终端上），新的 `Live` 容器接管输出，以 **10Hz 的刷新率**渲染增量更新的 Markdown 内容。这里使用 `Live.update(Markdown(full_response))` 全量替换而非追加，是因为 Markdown 渲染需要完整的文档树才能正确呈现标题层级和代码块结构。

切换完成后，进度行变为固定的历史信息，AI 回复区域开始实时展开。**两套 Live 实例互斥运行**，确保任何时候终端中只有一个活跃的 Live 容器。

Sources: [agent/agent_langchain.py#L1037-L1055](agent/agent_langchain.py#L1037-L1055)

## 上下文用量可视化：20 格的色彩预警

每次对话结束后，`format_context_bar()` 静态方法会生成一个带有颜色渐变和精确数值的上下文用量条。该方法的输入来自 `get_context_usage()`，后者通过 `count_tokens_approximately()` 估算当前完整上下文的 Token 消耗。

```python
# 颜色梯度逻辑
if pct < 50:    color = "green"   # 安全区
elif pct < 80:  color = "yellow"  # 警戒区
else:           color = "red"     # 危险区
```

实际的呈现效果为：`|████████░░░░ 45% (58K/128K)`，其中方块字符 `█` 和 `░` 组成 20 格的进度条，数值自动适配单位（K/M），颜色随用量百分比从绿渐变到红。这一设计让用户**零认知负担**地感知上下文窗口的压力状况，及时意识到是否需要 `clear` 清理历史。

`get_context_usage()` 的实现使用了 `chars_per_token=2.0` 作为中英文混合场景的估算系数（Rich 默认的 `count_tokens_approximately` 使用 4.0，偏向纯英文），使估算值更接近中文模型的实际消耗。

Sources: [agent/ui.py#L278-L318](agent/ui.py#L278-L318), [agent/agent_langchain.py#L907-L930](agent/agent_langchain.py#L907-L930)

## 智能输入系统：prompt_toolkit 的深度集成

`PocketUI` 在交互输入上选择了 `prompt_toolkit` 而非 Rich 自带的输入方案，这是基于以下考量：

| 需求 | Rich 方案 | prompt_toolkit 方案（已选） |
|------|-----------|---------------------------|
| 历史记录回溯 | 不原生支持 | `FileHistory` 持久化到 `~/.pocket_agent_history` |
| Tab 自动补全 | 不原生支持 | `SlashCompleter` 自定义补全 |
| 编辑键支持（方向键/退格） | 有限 | 完整支持 |
| 异步输入 | 不原生支持 | `prompt_async()` 原生异步 |
| ESC 打断 | 需手动实现 | `KeyBindings` 绑定 |
| 多行输入 | 支持 | 支持 |

`SlashCompleter` 实现了两级补全逻辑：当用户输入以 `/` 开头时，优先匹配 `/resume`、`/undo`、`/clear` 三个内置命令；若未命中命令但输入了部分文字，则匹配已加载的技能名称。技能列表通过 `update_skill_completions()` 在程序启动时注入，确保补全内容与当前加载的技能集一致。

异步版本 `async_print_user_input_prompt()` 直接调用 `prompt_session.prompt_async()`，不会阻塞事件循环，这对于需要同时处理 SSE 推送或后台任务的场景（如 `main.py` 中的浏览器交互模式）至关重要。

Sources: [agent/ui.py#L30-L220](agent/ui.py#L30-L220)

## 子 Agent 执行中的 Rich 输出集成

除了主对话流程，UI 系统还深度参与了子 Agent（Executor）的前台执行。在 `_run_executor_foreground()` 方法中，工具调用和执行状态通过 `self.ui.console.print()` 打印到终端：

```
▶ 子Agent执行: 查看当前时间
  🔧 shell_exec(date)
  ✅ shell_exec 完成
  ✅ 完成，⏱️ 3.2s
```

`_render_task_progress()` 方法将子 Agent 任务清单渲染为带状态符号的多行文本（✔ 完成、⏳ 进行中、✘ 失败、⤾ 跳过、◻ 等待），这些状态行通过 `console.print()` 逐步输出，形成可读的执行日志。

当子 Agent 请求人工介入时（`await_user_input`），系统通过醒目的黄色 Panel 提示用户输入指令；介入完成后以绿色标记继续执行。整个执行链路的每个关键节点都有对应的视觉反馈。

Sources: [agent/agent_langchain.py#L1577-L1883](agent/agent_langchain.py#L1577-L1883)

## 安全的生命周期管理：三层防护

Rich 的 Live 组件在异常情况下可能造成终端混乱，为此系统设计了**三层防护机制**：

1. **语法层（Context Manager）**：`ProgressDisplay` 实现 `__enter__`/`__exit__` 协议，正常流程下由 `with` 语句自动管理生命周期。

2. **异常层（Exception Handler）**：`run_conversation()` 中的三个 `except` 分支（`CancelledError`、`KeyboardInterrupt`、`Exception`）各自独立处理 Live 的关闭，使用 `locals()` 检查变量是否已绑定，避免 `UnboundLocalError`。

3. **防御层（Nested try/except）**：每个关闭操作都包裹在内部的 `try/except` 中，防止关闭过程本身抛出异常。

```python
except Exception as e:
    if 'progress_display' in locals() and progress_display:
        try:
            progress_display.__exit__(type(e), e, None)
        except:
            pass
    if 'stream_live' in locals() and stream_live:
        try:
            stream_live.__exit__(None, None, None)
        except:
            pass
```

这种层层防护的设计确保了**即使在最恶劣的异常条件下，终端也不会处于 Live 悬挂的混乱状态**——用户不会看到光标消失或终端输出异常的景象。

Sources: [agent/agent_langchain.py#L1211-L1248](agent/agent_langchain.py#L1211-L1248)

## 性能考量与边界

Rich 库在终端渲染上是高效的，但在移动端（如 Termux）上仍有一些注意事项：

- **Live 刷新率**：4Hz 对于进度条足够，10Hz 对于 Markdown 流式渲染也流畅。大幅提高刷新率（>30Hz）在 Termux 中可能导致渲染卡顿，因为 Android 终端模拟器的输出管道吞吐量有限。
- **Markdown 全量重渲染**：每次收到新 token 都执行 `Live.update(Markdown(full_response))`，这意味着每次都是全量重渲染。对于长达数万字的回复，可以考虑在段落边界触发更新而非每个 token，但当前设计更注重渲染的即时性。
- **Panel 嵌套开销**：`print_tool_call` 和 `print_tool_result` 使用 Panel 包装内容，当工具调用频繁时（如几十次连续调用），Panel 的边框绘制会略微增加输出行数。这在正常使用场景下（通常少于 10 次工具调用）不是问题。

整体而言，这套 UI 系统在功能性上覆盖了终端交互的每一个环节，在健壮性上考虑了异常退出的每一种路径，在用户体验上通过色彩、边框和实时进度条将原本枯燥的终端输出提升为具有视觉层级的信息仪表盘。

Sources: [agent/ui.py](agent/ui.py), [agent/agent_langchain.py](agent/agent_langchain.py)

---

**继续阅读**：[结构化日志系统](44-jie-gou-hua-ri-zhi-xi-tong-an-tian-gui-dang-dui-hua-gong-ju-zi-agent-quan-lian-lu-shen-ji) — 与终端 UI 互补的后台审计链路