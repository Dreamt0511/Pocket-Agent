当主 Agent 遇到需要多步手机操控的复杂任务（点击、滑动、UI 树分析、输入等）时，系统不会让主 Agent 亲自逐步骤执行——而是通过 `delegate_task()` 工具将任务**全盘委托**给一个独立的子 Agent（Executor）在后台执行。这种"主 Agent 决策 + 子 Agent 执行"的职责分离架构，是 Pocket-Agent 处理真实世界手机自动化任务的基石。

其核心理念在于：主 Agent 负责**理解意图、分解任务、管理对话**，子 Agent 负责**专注执行、操作手机、沉淀经验**。两者分工明确，通过文件系统契约（task.json）和解耦的任务队列完成协作。

Sources: [agent/tools/basic_tools.py#L898-L918](agent/tools/basic_tools.py#L898-L918), [agent/prompts/agent_enhance.py#L33-L43](agent/prompts/agent_enhance.py#L33-L43)

## 架构概览：三层流水线

```mermaid
flowchart TD
    User["用户请求<br/>（如：在拼多多买鞋）"] --> MainAgent["主 Agent<br/>（LangChain create_agent）"]

    MainAgent --> Judge{"判断任务类型"}

    Judge -->|"简单操作<br/>单步 TermUX API"| ShellExec["shell_exec<br/>直接执行"]
    Judge -->|"复杂操作<br/>多步手机交互"| Delegate["调用 delegate_task()"]

    Delegate --> TaskQueue["全局任务队列<br/>_pending_background_tasks"]
    Delegate --> TaskFile["创建 task.json<br/>/tasks/task_xxx/"]

    TaskQueue -->|"主 Agent 流式响应结束后"| Consume["消费队列"]

    Consume --> Executor["创建子 Agent Executor<br/>独立 LLM + 独立工具集 + 独立提示词"]

    Executor --> ReadTask["读取 task.json<br/>理解目标与步骤"]
    Executor --> Execute["按原子步骤<br/>逐条执行"]
    Execute -->|"每步执行后"| UpdateTask["更新 task.json<br/>标记步骤状态"]

    Execute --> Success{"是否成功？"}
    Success -->|"是，步骤≥3"| Condense["自动沉淀技能<br/>→ auto-skills/executor/"]
    Success -->|"否，步骤≤2"| Retry["重试机制<br/>最多 2 次"]
    Success -->|"彻底失败"| Report["语音通知用户<br/>记录失败状态"]

    Retry --> Execute
```

上图展示了完整的委托生命周期。从用户请求进入主 Agent 开始，到任务完成后的技能沉淀结束。主 Agent 仅在**判断任务类型**和**分解步骤**两个环节参与——一旦调用 `delegate_task()`，后续所有操作手机的行为均由子 Agent 独立完成。这种流水线设计确保主 Agent 不会被复杂的手机操作拖慢响应速度，用户可以继续与之对话。

Sources: [agent/agent_langchain.py#L1122-L1134](agent/agent_langchain.py#L1122-L1134), [agent/tools/basic_tools.py#L905-L965](agent/tools/basic_tools.py#L905-L965)

## 委托触发条件：什么任务需要派发？

系统提示词为主 Agent 定义了清晰的任务分类标准，这是一个二值判断：

| 任务类型 | 特征 | 处理方式 | 示例 |
|---------|------|---------|------|
| **简单操作** | 单步 Termux API、查系统信息、包管理 | `shell_exec` 直接执行 | 发通知 `termux-notification`、查电池 `termux-battery-status`、查属性 `getprop` |
| **复杂操作** | 需要点击、输入、滑动、UI 树分析等多步交互 | `delegate_task()` 全盘派发 | 启动应用、搜索商品、填写表单、截图分析 |

关键约束是：**一旦判断需要派发给子 Agent，主 Agent 不允许自己做任何准备工作**——包括查包名、启动应用等。整个任务必须全盘委托，子 Agent 会从头开始自己处理。这是因为主 Agent 的工具集与子 Agent 的工具集不同（主 Agent 没有 `mcp_call` 等手机操控工具），任何准备工作都可能因为权限或工具缺失而失败。

Sources: [agent/prompts/agent_enhance.py#L32-L38](agent/prompts/agent_enhance.py#L32-L38)

## 任务分解协议：原子粒度的 Steps

`delegate_task()` 工具接受两个核心参数：`description`（自然语言描述）和 `tasks_json`（结构化任务定义）。系统提示词**强制要求** `tasks_json` 中的 `steps` 必须按原子粒度分解——每个步骤是单步操作，禁止将多步合并：

```python
# ✅ 正确：原子粒度分解
tasks_json = '''{
    "objective": "在拼多多买一双黑色的鞋",
    "steps": [
        {"id": 1, "desc": "打开拼多多APP"},
        {"id": 2, "desc": "在搜索框输入'黑色鞋子'"},
        {"id": 3, "desc": "筛选价格100-200元"},
        {"id": 4, "desc": "选择第一件商品进入详情"},
        {"id": 5, "desc": "咨询客服是否有货"},
        {"id": 6, "desc": "汇报完成情况"}
    ],
    "guidance": "优先选择包邮商品，不要倍速"
}'''

# ❌ 错误：多步合并，子Agent无法执行
tasks_json = '{"objective":"在拼多多买鞋","steps":[{"id":1,"desc":"去拼多多买件衣服"}]}'
```

`tasks_json` 支持三个顶层字段：`objective`（任务目标，写清楚做什么和做到什么程度算完成）、`steps`（原子步骤数组，每个必须有 `id`、`desc`、`status` 字段，`status` 初始为 `"pending"`）、`guidance`（可选的注意事项）。最后一个步骤应该设置为"汇报完成情况"或"保存结果"，确保任务有明确的收尾条件。

如果主 Agent 未提供 `tasks_json`，或 `steps` 为空，系统会回退为将整段 `description` 作为单个步骤——但这通常会导致子 Agent 无法正确执行，因此提示词系统对此有严格的警告。

Sources: [agent/tools/basic_tools.py#L918-L975](agent/tools/basic_tools.py#L918-L975), [agent/prompts/agent_enhance.py#L48-L60](agent/prompts/agent_enhance.py#L48-L60)

## 异步委托 + 前台执行：双阶段运行模型

`delegate_task()` 的调用是异步的——它只是将任务写入文件、加入队列，然后立即返回给主 Agent。主 Agent 可以继续回复用户，用户不会感知到后台有任何操作。真正执行发生在**当前对话的流式响应结束后**：

```
主 Agent 响应流程：
1. 用户输入 → 2. 主线 Agent 流式推理 → 3. 调用 delegate_task()（入队，立即返回）
        → 4. 继续回复用户（告诉用户任务已派发）→ 5. 流式响应结束
        → 6. 消费 pending_tasks 队列 → 7. 子 Agent 同步执行（前台，带 Rich UI 进度）
        → 8. 清理 delegate_task 相关消息 → 9. 返回最终响应
```

第 6-8 步发生在主 Agent 的 `run_conversation` 方法末尾，在 `finally` 块之前执行。这意味着子 Agent 实际上是**串行阻塞**主线程的——虽然它对用户来说是"后台"，但在进程层面它是前台执行的。这种设计避免了并发 Agent 执行带来的状态管理和资源竞争问题。

子 Agent 执行期间，系统通过 Rich Live 显示实时进度（终端模式）或通过 SSE 事件推送（HTTP API 模式）。每个工具调用都会触发 `tool_start`/`tool_end` 事件，子 Agent 的思考过程（reasoning_content）和输出 token 也会实时推送。

Sources: [agent/agent_langchain.py#L1122-L1134](agent/agent_langchain.py#L1122-L1134), [agent/agent_langchain.py#L1596-L1605](agent/agent_langchain.py#L1596-L1605)

## 子 Agent 的独立配置与隔离

每次委托任务都会创建一个全新的子 Agent 实例，拥有完全独立的运行时环境。这种隔离是通过多层机制保证的：

**LLM 配置独立**：子 Agent 可以从 `.env` 文件读取 `EXECUTOR_LLM_BASE_URL`、`EXECUTOR_API_KEY`、`EXECUTOR_MODEL` 等配置，未设置的字段自动继承主 Agent 的对应值。这意味着可以为子 Agent 指定一个更便宜或更快的模型（如 4B 小模型），降低运行成本。LLM 实例在 `_executor_llm` 属性中缓存，复用同一个客户端连接，避免每个任务创建新的 HTTP 连接。

**工具集限制**：子 Agent 只能使用 `shell_exec`、`file_read`、`file_write`、`file_search`、`mcp_call`、`system_info`、`tts_speak` 这 7 个工具。它没有 `delegate_task`（不能再次委托）、`save_memory`（不负责记忆管理）、`update_user_profile`（不修改用户画像）等工具——这些是主 Agent 的专属能力。

**提示词隔离**：子 Agent 拥有独立的系统提示词系统。`executor_system_prompt` 定义其基本人设（"手机操控执行助手"）和强制规则，`executor_enhance_prompt` 在运行时填充工具列表和技能列表后注入。这种两层结构确保核心规则不可篡改，同时技能和工具信息可以动态更新。

**会话隔离**：子 Agent 使用独立的 `MemorySaver`（而非主 Agent 的 `AsyncSqliteSaver`），使用独立的 `thread_id`（格式为 `executor_{task_id}`）。这意味着子 Agent 的执行历史不会污染主 Agent 的对话状态，也不会持久化到 SQLite 中。

```python
# 关键隔离代码示意
executor_agent = create_agent(
    model=executor_llm,          # 独立 LLM 实例
    tools=executor_tools,        # 限制后的工具集（7个）
    system_prompt=system_content, # 独立系统提示词
    checkpointer=MemorySaver(),  # 独立内存检查点（不持久化）
    middleware=[                  # 独立中间件链
        ToolCallIdMiddleware(),
        ModelCallLimitMiddleware(run_limit=max_iterations)
    ],
)
```

Sources: [agent/agent_langchain.py#L1606-L1650](agent/agent_langchain.py#L1606-L1650), [agent/config.py#L35-L45](agent/config.py#L35-L45)

## 执行流程：从读取 task.json 到技能沉淀

子 Agent 的执行流程由 `_run_executor_foreground` 方法驱动，这是一个完整的异步生成器：

**阶段一：初始化与启动**（第 1596-1680 行）。方法接收 `task_path` 和 `task_id`，创建 executor Agent 实例，读取 `task.json` 获取任务目标和步骤，记录开始日志，yield `executor_start` 事件。如果用户画像包含"语音"偏好，还会通过 `termux-tts-speak` 语音播报任务开始。

**阶段二：流式执行**（第 1698-1770 行）。子 Agent 以 `astream` 模式执行，主循环处理两种流式数据：`messages` 类型携带模型输出的 token 和推理内容（reasoning_content），`updates` 类型携带工具调用开始和结束事件。每个工具调用都会被记录到日志（`log_executor_step`）并通过 yield 推送给 UI 或 SSE。遇到 `tts_speak` 工具调用时，标记为"人工介入"状态——系统等待用户手动处理，然后继续执行。

**阶段三：重试机制**（第 1784-1865 行）。如果子 Agent 执行了 ≤2 步（仅读了 task.json 但没有执行有效操作），系统会自动重试。重试使用**全新的 Agent 实例**（`fresh_agent`）和**内联的任务指令**（不再依赖 `task.json` 文件读取），避免复用原对话线程导致的 DashScope API 400 错误（tool_call/tool_message 配对问题）。最多重试 2 次。

**阶段四：完成与汇报**（第 1868-1908 行）。重新从磁盘读取 `task.json`（子 Agent 可能通过 `file_write` 修改了 steps 状态），检查是否有失败的步骤。将所有非 completed 的步骤统计为失败，标记 `all_completed` 状态。统计 token 消耗（通过 `count_tokens_approximately`），yield `executor_done` 事件。如果执行过程中有失败步骤，`status` 为 `completed_with_failures`，否则为 `completed`。

**阶段五：技能沉淀**（第 1935-2030 行）。当步骤数 ≥ 3 且没有失败时，系统自动触发技能沉淀。子 Agent 收到一条 followup 指令，要求将本次执行路径编写为 SKILL.md 存入 `auto-skills/executor/<技能名称>/`。系统会检查生成的技能文件是否合规（必须有 frontmatter `---`、`name:`、`description:`），不合规则要求子 Agent 先读取 `skill-creator/SKILL.md` 模板后重写。

Sources: [agent/agent_langchain.py#L1596-L2030](agent/agent_langchain.py#L1596-L2030)

## 消息清理机制：防止 API 错误

子 Agent 执行完成后，系统会清理主 Agent 状态中的 `delegate_task` 相关消息。这是因为某些 LLM API（如 DashScope）对消息历史中的 tool_call/tool_message 配对有严格校验——如果保留 `delegate_task` 的调用记录而删除对应的子 Agent 执行消息，下次对话时可能导致 `400 Bad Request` 错误。

清理逻辑（第 1136-1175 行）通过 `aget_state` 获取当前状态，遍历所有消息，找出调用了 `delegate_task` 的 `AIMessage` 以及对应的 `ToolMessage`，收集它们的 ID 后使用 `RemoveMessage` 从状态中移除。这个操作通过 `update_state` 写入，不影响之后的对话上下文。

```python
# 清理策略
for m in msgs:
    if hasattr(m, 'tool_calls') and m.tool_calls:
        for tc in m.tool_calls:
            if tc.get('name') == 'delegate_task':
                to_remove_ids.add(m.id)              # 删除主 Agent 的 AIMessage
                delegate_tc_ids.add(tc.get('id', '')) # 记录工具调用ID
for m in msgs:
    if isinstance(m, ToolMessage) and m.tool_call_id in delegate_tc_ids:
        to_remove_ids.add(m.id)                      # 删除对应的 ToolMessage
```

这意味着 `delegate_task` 的调用记录不会进入下一轮对话——子 Agent 的执行结果（成功/失败）通过 `_background_task_results` 列表和 `task.json` 文件持久化，主 Agent 在下次对话时可以通过 `file_read` 查看，而不是依赖对话历史。

Sources: [agent/agent_langchain.py#L1136-L1175](agent/agent_langchain.py#L1136-L1175)

## 错误处理与人工介入

子 Agent 在执行过程中可能遇到各种错误，系统设计了多层次的错误处理机制：

**工具调用失败重试**（由 executor 提示词控制）：同一个方案失败后最多换 1 个替代方案再试，仍然失败则通过 `tts_speak` 通知用户并等待 30 秒。用户在此期间可以手动操作手机，等待结束后子 Agent 继续执行。

**Agent 级别异常**（第 2038-2093 行）：如果子 Agent 抛出未捕获的异常，系统会记录详细的错误栈到日志，标记 `task.json` 为失败状态，通过 `termux-tts-speak` 语音播报失败原因，并将失败结果记录到 `_background_task_results` 列表。下次对话时主 Agent 可以查看这个列表并告知用户。

**取消机制**（第 2038 行）：系统在子 Agent 执行循环中检查 `_check_cancel()` 方法——如果主 Agent 收到取消信号（如用户输入 /cancel），子 Agent 会立即中断执行。

所有异常路径都会 yield `executor_done` 事件（携带 `status: "failed"` 或 `"cancelled"`），确保调用方（无论是终端 UI 还是 HTTP SSE 流）都能收到明确的完成信号。

Sources: [agent/agent_langchain.py#L1904-L2093](agent/agent_langchain.py#L1904-L2093)

## 总结：设计哲学

`delegate_task` 机制的设计体现了几项核心原则：

**职责分离**：主 Agent ≠ 执行 Agent。主 Agent 专注于人类交互——理解意图、分解任务、管理记忆、回复用户；子 Agent 专注于机器交互——操作手机、处理错误、沉淀经验。这种分离让两个 Agent 可以使用不同的模型、不同的提示词、不同的工具集，针对各自的任务特性进行优化。

**通过文件契约协作**：主 Agent 与子 Agent 之间不直接通信。`task.json` 是唯一的共享状态——主 Agent 写入任务定义，子 Agent 读取执行并更新进度。这种异步、松耦合的设计避免了共享内存带来的复杂状态同步问题。

**执行即学习**：每次成功执行都会自动沉淀为技能文档（SKILL.md）。这意味着同一个操作（如"在拼多多买鞋"）第一次执行后，后续再次执行时子 Agent 可以直接读取已有技能，复用包名、坐标、操作路径——执行速度会随着使用次数增加而提升。

**隔离胜于共享**：子 Agent 拥有独立的所有资源——LLM 实例、工具集、提示词、检查点、对话线程。这种"从头创建、用完即弃"的模式虽然开销较大，但彻底避免了状态污染和跨 Agent 副作用。

Sources: [agent/agent_langchain.py#L1596-L1650](agent/agent_langchain.py#L1596-L1650), [agent/tools/basic_tools.py#L905-L975](agent/tools/basic_tools.py#L905-L975)

---

**延伸阅读**：详细了解子 Agent 的前台执行细节，请参见 [子 Agent 前台同步执行与流式输出](30-zi-agent-qian-tai-tong-bu-zhi-xing-yu-liu-shi-shu-chu)；关于子 Agent 的独立配置与技能隔离，请参见 [子 Agent 独立配置与技能隔离](31-zi-agent-du-li-pei-zhi-yu-ji-neng-ge-chi)；关于任务文件管理与进度追踪，请参见 [任务文件管理与进度追踪](32-ren-wu-wen-jian-guan-li-yu-jin-du-zhui-zong)；关于技能沉淀的完整机制，请参见 [自动沉淀技能（auto-skills）](36-zi-dong-chen-dian-ji-neng-auto-skills-zhi-xing-jing-yan-zi-dong-ji-lei)。